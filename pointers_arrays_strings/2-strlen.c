#include "main.h"
/**
 * _strlen - function that count the number of chars in one string
 * @s: value of a pointer
 * Return: i
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}
	return (i);
}

